extends ./layout/layout.pug

block content
    div(class="container-fluid")
        h1 Bienvenidos
        p Puede agregar un producto desde el siguiente formulario o dirijase a Productos para ver aquellos disponibles.
        form(action="/api/productos", method="POST", class="d-flex flex-column", style="width:300px")
            p Titulo:
            input(type="text", name="title")
            p Precio:
            input(type="number", name="price")
            p URL Foto:
            input(type="text", name="thumbnail")
            input(type="submit", value="Agregar", class="btn btn-secondary mt-2")